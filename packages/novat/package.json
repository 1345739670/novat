{
  "name": "@novat/novat",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "rimraf ../../lib && vue-tsc --noEmit && vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Klein",
  "license": "ISC",
  "dependencies": {
    "@novat/button": "workspace:^"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "vite-plugin-dts": "^3.5.2"
  }
}
